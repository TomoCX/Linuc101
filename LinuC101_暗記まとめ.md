# LinuC レベル1 101試験 暗記まとめ

レクチャースライド **101〜149**（101試験の出題範囲）の内容を、暗記すべき項目に絞って1ファイルにまとめたもの。
※ スライド150〜187（シェルスクリプト・ネットワーク・システム管理・セキュリティ・OSS）は **102試験** の範囲なのでここには含めない。141_UDP.pdf も内容的に102範囲。

## 出題範囲とスライドの対応

| 主題 | 内容 | 対応スライド |
|---|---|---|
| 1.01 | Linuxのインストールと仮想マシン・コンテナの利用 | 101〜113、116〜117 |
| 1.02 | ファイル・ディレクトリの操作と管理 | 118〜126 |
| 1.03 | GNUとUnixのコマンド | 114〜115、127〜135 |
| 1.04 | リポジトリとパッケージ管理 | 136〜140 |
| 1.05 | ハードウェア、ディスク、パーティション、ファイルシステム | 142〜149 |

---

# 主題1.01 Linuxのインストールと仮想マシン・コンテナの利用

## OSとカーネル・シェル

| 用語 | 意味 |
|---|---|
| OS (operating system) | ユーザー・アプリケーションとハードウェアの間に立ち、効率的な管理を行うソフトウェア |
| カーネル (Kernel) | OSの最も基本的な機能。ハードウェアとアプリケーションを管理。ユーザーが直接操作できない |
| シェル (Shell) | カーネルとユーザーをつなぐ**通訳係**。ユーザーはシェルを通してカーネルとやりとりする |

**OSの5つの機能**：ユーザー管理／ファイル管理／入出力管理／タスク管理／メモリ管理

**階層**（下から）：ハードウェア → カーネル → シェル → アプリケーション → ユーザー

## Linuxの基礎知識（数字と人名は出る）

- **1991年**、フィンランドの**ヘルシンキ大学**の学生 **リーナス・トーバルズ**氏が開発
- **UNIX** と同機能のOSを目指した（LinuxはUnix系OSの一つ）
- 本来「Linux」は OSの**カーネル部分**を指す
- フリー（無料）かつ**オープンソース**（ソースコード公開）
- マスコットは **タックス (TUX)**
- 1969年：AT&T ベル研究所で **Unics**（のちのUNIX）が開発

| ディストリビューション | 特徴 |
|---|---|
| RHEL (Red Hat Enterprise Linux) | 最も有名な商用向け。有料で**商用サポートが充実** |
| Ubuntu | **Debian** から派生。初心者向け。Ubuntuソフトウェアセンター |
| CentOS | RHELの完全互換を目指していたが方針変更。無償だが商用サポートなし |

**ディストリビューション**＝「配布・流通」の意。カーネルをもとに様々なソフトウェアを加えてまとめたもの。

| CUI (Character-based User Interface) | GUI (Graphical User Interface) |
|---|---|
| キーボードでコマンド入力 | マウスでも操作できる |
| 全て文字や記号で表示 | 絵やアイコンも表示 |
| 低スペックでも快適 | 高い性能が必要 |
| コマンドを覚える必要がある | 直感的に操作できる |

## インストールの手順（順序を覚える）

1. 配布サイトから **`.iso`** ファイルをダウンロード
2. isoファイルをCD/DVD-ROMにコピー（「焼く」）
3. 電源投入直後に **[F2] か [Delete]** で **BIOS / UEFI** セットアップ画面を表示
4. **Boot（ブート＝起動）** 関連項目で**光学ドライブの優先度を一番高く**する
5. セットアップ画面を閉じる（**設定の保存を忘れない**）
6. インストールメニューにしたがって設定
7. 再起動し、BIOS/UEFIでboot優先度を戻す

## 起動の流れとブートプロセス

```
電源が入る
 → BIOS / UEFI が起動   ・ハードウェアのチェック・初期化
                        ・起動デバイスのブートローダーを読み出す
 → ブートローダーがカーネルをメモリに読み込む
 → カーネル             ・メモリ初期化、システムクロック設定
                        ・仮のルートファイルシステムをマウント
                        ・初期RAMディスクのデバイスドライバでデバイスにアクセス
 → init または systemd  ・起動に必要なものを順次起動
                        ・ログインプロンプトを表示して起動完了
```

## シャットダウン（shutdown）

`shutdown [オプション] 時刻 "メッセージ"` の順で書く。

| オプション | 意味 |
|---|---|
| `-h` , `-P` [`--poweroff`] | システムを終了して**電源を切る** |
| `-H` [`--halt`] | システムを終了させる |
| `-r` [`--reboot`] | システムを終了して**再起動** |
| `-k` | 終了せず**メッセージだけ**を送る |
| `-c` | システム終了を**キャンセル** |
| `--no-wall` | シャットダウン/キャンセル時にメッセージを表示しない |

**時刻の指定**：`now`（今すぐ）／指定なし（1分後）／`20:40`（時刻指定）／`+10` または `10`（10分後）

- オプション省略時は **`-P` と同じ**（電源オフ）
- 電源を切る他の方法：**`halt -p`** / **`poweroff`**
- 再起動の他の方法：**`reboot`**
- 基本的に **rootユーザー**しか使えない

```
# shutdown -h +5 "shutdown in 5 minutes"   → 5分後に電源オフ、メッセージ送信
# shutdown -c                              → 予定をキャンセル
```

## SSH

**SSH (Secure Shell)** ＝ネットワーク上の通信を**暗号化**して安全に操作する仕組み。主に **OpenSSH** を使う。

| 認証方式 | 特徴 |
|---|---|
| パスワード認証方式 | 設定が簡単／安全性が低い（パスワード流出のリスク） |
| 公開鍵認証方式 | 安全性が高い／設定が難しい |

### SSH関連ファイル（どこに置かれるかが頻出）

**クライアント側**（ユーザーごとに `~/.ssh/` 、共通は `/etc/ssh/`）

| ファイル | 内容 |
|---|---|
| `ssh_config` | クライアント側のSSHの設定 |
| `id_rsa` | SSHの**秘密鍵** |
| `id_rsa.pub` | SSHの**公開鍵** |
| `known_hosts` | 接続先サーバの情報／送られてきたホスト認証用の公開鍵 |

**ホスト（サーバ）側**（基本は `/etc/ssh/` ）

| ファイル | 内容 |
|---|---|
| `sshd_config` | ホスト側のSSHの設定 |
| `ssh_host_rsa_key` | ホスト側の**秘密鍵** |
| `ssh_host_rsa_key.pub` | ホスト側の**公開鍵** |
| `authorized_keys` | クライアントが登録した公開鍵のリスト。**これだけ `~/.ssh/` に置かれる** |

※ `~/.ssh/ssh_config`（ユーザー個別）のほうが `/etc/ssh/ssh_config` より**優先度が高い**

### SSHのコマンド（順序で覚える）

| 手順 | コマンド | 内容 |
|---|---|---|
| ① 鍵ペア作成 | `ssh-keygen` | 公開鍵と秘密鍵を `~/.ssh` に作成。入力するパスフレーズは**秘密鍵を使うためのもの** |
| ② 公開鍵を登録 | `ssh-copy-id ユーザー@ホスト` | サーバ側 `~/.ssh/authorized_keys` に書き込む。登録には他の認証（パスワード等）が有効である必要あり |
| ③ ログイン | `ssh ユーザー@ホスト` | ホスト名の代わりにIPアドレスも可 |
| ④ ログアウト | `logout` / `exit` | **Ctrl + D** でも可 |

## 仮想マシン

**仮想マシン**＝実際のコンピュータ上に構築された仮想的なコンピュータ

- メリット：何台もサーバを用意しなくていい／一台で複数サーバを操作／様々な環境を手軽に導入
- デメリット：本来の性能を活かせない／物理環境の不具合の影響が大きい

| 方式 | 特徴 |
|---|---|
| **ホストOS型** | 仮想化ソフトをホストOS上にインストール。Windows/Macに手軽に導入できる（例：**VirtualBox**＝オラクル社）。性能を活かしきれない |
| **ハイパーバイザ型** | Linuxで用いられる方式。**CPUに仮想化支援機能**が必要。効率的に稼働できる |

### KVM と virsh

**KVM (Kernel-based Virtual Machine)** …**Linuxカーネルに標準で組み込まれた**仮想化システム。**ハイパーバイザ型**。CPUの対応が必要。

CPU対応の確認（**VT-x**＝Intel／**AMD-V**＝AMD）

```
$ lscpu | grep Virtualization        → 「VT-x」等が表示されればOK
$ grep -E 'vmx|svm' /proc/cpuinfo    → 「vmx」か「svm」があればOK
```

`virsh` の主なサブコマンド

| サブコマンド | 内容 |
|---|---|
| `create` | xmlファイルから仮想マシンを作成 |
| `start` | 仮想マシンを起動 |
| `resume` | 再開 |
| `shutdown` | 正常に終了 |
| `destroy` | **強制終了** |
| `suspend` | 一時停止 |
| `reboot` | 再起動 |
| `console` | コンソールに接続 |
| `dumpxml` | 設定をxmlファイルで出力 |
| `list` | 稼働中の一覧を表示（**`--all` で停止中も含めた全て**） |

**libvirt**＝仮想マシンの管理を便利にするソフトウェアの集まり。「library ＋ virtualization」。**Cライブラリ**中心。様々な仮想化に対応。

## コンテナ

**コンテナ**＝アプリケーションを仮想的に**環境ごと独立**させて管理する仕組み。**コンテナエンジン**で作成・管理。

- 仮想マシンと違い、**OS（カーネル）やハードウェアの基本設定は共有**する
- **cgroups** … コンテナごとの**リソース**を管理
- **namespace** … コンテナごとの**ユーザー権限など**を管理
- メリット：互いに独立して干渉しない／複製が簡単／必要な環境ごと複製できる／別マシンに移しても安定

### Docker

- **Dockerレジストリ / Docker Hub** … 様々なDockerイメージがWeb上に保存・公開されている

| サブコマンド | 内容 |
|---|---|
| `pull` | Dockerハブからイメージを**ダウンロード** |
| `push` | Dockerハブにイメージを**アップロード** |
| `run` | 新しいコンテナを作成して実行 |
| `ps` | （稼働中の）コンテナの一覧（**`-a` で終了したものも含め全て**） |
| `images` | Dockerイメージの一覧 |
| `rm` | コンテナを削除 |
| `start` / `stop` / `kill` | 起動／停止／**強制停止** |
| `attach` | コンテナの標準入出力に接続して操作 |
| `exec` | コンテナでコマンドを実行 |

```
# docker pull centos:8
# docker run -it --name centOStest centos:8 /bin/bash
```

- コンテナ接続状態から一時的に抜ける：**Ctrl + P** → **Q**

## systemd

**systemd** … Linuxの起動処理やシステム管理を行う仕組み。

- 従来の **init（Sysvinit）** の置き換えとして開発された
- initより**高速**に起動/停止できる
- **cgroups** でプロセスのリソースを管理
- Sysvinitはシェルスクリプトにまとめて書かれていて細かい変更が難しかった

**ユニット (Unit)** … systemdで扱われる基本的な処理の単位（＝設定ファイル）。設定ファイルが1つずつ分かれているので変更が簡単。
種類：`~.target` `~.mount` `~.service` `~.device` `~.swap`

**ターゲット** … 複数のUnitをまとめたもの

| ターゲット名 | 説明 |
|---|---|
| `runlevel3.target` | ほとんどのプロセスが動作するモード（Webサーバー運用時に指定） |
| `multi-user.target` | **CUI**ログインを行う |
| `graphical.target` | **グラフィカル（GUI）**ログインを行う |
| `poweroff.target` | システムを終了する |
| `rescue.target` | レスキュー（**シングルユーザー**）モード |
| `reboot.target` | システムの再起動を行う |

`systemctl` の主なサブコマンド

| サブコマンド | 内容 |
|---|---|
| `isolate` | 他のUnitを停止して、指定したUnitを起動する |
| `status` | サービスの状態を表示 |
| `is-active` | サービスが稼働しているか表示 |
| `enable` / `disable` | **自動起動**を有効／無効にする |
| `start` / `stop` | 起動／停止 |
| `restart` | 再起動（設定ファイルは**再読み込みしない**） |
| `reload` | 再起動（設定ファイルを**再読み込みする**） |
| `set-default` / `get-default` | 次回起動時のターゲットを設定／表示 |
| `reboot` | システムを再起動 |
| `list-unit-files` | 利用可能なユニットの一覧 |

## デスクトップ環境と X Window System

**X Window System** … LinuxでGUI環境を実現している仕組み。実装ソフトウェアは **X.Org Server**。

**サーバとクライアントの向きが逆になるので注意**

| | 役割 |
|---|---|
| **Xサーバ** | **入出力装置**（ディスプレイ・マウス・キーボード）を管理する側 |
| **Xクライアント** | 入力情報が欲しい・ディスプレイに表示してほしい**アプリケーション** |

ネットワーク経由の場合：**ローカルホスト（ユーザー側）＝Xサーバー**、**リモートホスト（サーバー側）＝Xクライアント**

| ソフトウェアの種類 | 役割 |
|---|---|
| ウィンドウマネージャ | X Window Systemの**外観**を制御する |
| 統合デスクトップ環境 | WMや各種アプリをまとめ、統一された見た目・使い勝手を提供 |
| ディスプレイマネージャ | GUI環境で**ログイン画面を表示しユーザー認証**を行う。使用するデスクトップ環境を選択できる |

**代表的なウィンドウマネージャ**

| 名前 | 説明 |
|---|---|
| twm | 最小限の機能を持つ基本的なWM |
| mwm | 単独で機能する（スタンドアローン）WM |
| fvwm | 立体的なウィンドウ表示を行うWM |
| Enlightenment | 幅広いカスタマイズが可能なWM |
| kWin | **KDE**の標準WM |
| Metacity | **GNOME 2** の標準WM |
| Mutter | **GNOME 3** の標準WM |
| WindowMaker | 標準的な高機能WM |

**代表的な統合デスクトップ環境**

| 種類 | KDE Plasma | GNOME | Xfce |
|---|---|---|---|
| テキストエディタ | KEdit | gedit | Mousepad |
| 端末 | Konsole | GNOME端末 | xfce4端末 |
| ファイルマネージャ | Dolphin | Nautilus | Thunar |
| ウィンドウマネージャ | KWin | Mutter | xfwm4 |
| ディスプレイマネージャ | SDDM | GDM | XFCE |

### X の起動とリモート利用

```
$ startx                                  → X Window System を起動
# systemctl isolate graphical.target      → GUI環境（graphical.target）を実行
```

SSH経由でXを使う手順

1. リモートホスト側の **`/etc/ssh/sshd_config`** に **`X11Forwarding yes`** と記述
2. リモートホスト側でSSHサーバーを再起動 `# systemctl restart sshd.service`
3. ローカルから **`ssh -X`** で接続
4. **`echo $DISPLAY`** で DISPLAY環境変数を確認（書式：`[ホスト名またはIP]:ディスプレイ番号.スクリーン番号`）
5. **`xauth list`** で接続資格情報（MIT-MAGIC-COOKIE-1）を確認。この情報がリモート側の **`~/.Xauthority`** に記述されている必要がある
6. `$ xeyes &` などを実行 → リモートで実行、**ローカルのディスプレイに出力**される

---

# 主題1.02 ファイル・ディレクトリの操作と管理

## 所有者と所有グループ

| コマンド | 内容 | 主なオプション | 注意 |
|---|---|---|---|
| `chown ユーザー[:グループ] ファイル` | 所有者を変更 | `-R` [`--recursive`] | **rootユーザーしか変更できない** |
| `chgrp グループ ファイル` | 所有グループを変更 | `-R` [`--recursive`] | 一般ユーザーは**自分が所属するグループ間でしか**変更できない。rootは何でも可 |

所有者・所有グループの確認は **`ls -l`**。

## アクセス権（パーミッション）

| 権限 | 記号 | 数字 | 覚え方 | ファイルでできること | ディレクトリでできること |
|---|---|---|---|---|---|
| 読み取り | `r` (read) | **4** | 「読む（４む）」 | 内容を見る | 中のファイル/サブディレクトリ一覧を読み取る |
| 書き込み | `w` (write) | **2** | 「two」 | 内容の変更・削除 | 中にファイルを作成・削除 |
| 実行 | `x` (execute) | **1** | 「実行するワン」 | ファイルを実行 | 中のファイルにアクセスできる |

**表記の読み方**

```
-rwxr-xr--  testuser testuser 30 Jan 01 08:30 sample.txt
↓  ↓   ↓   ↓
種別 所有者 グループ その他のユーザー
     rwx=7  r-x=5    r--=4      → 754
```

**ファイル種別**：`-` ファイル／`d` ディレクトリ／`l` リンク

### chmod

`chmod [オプション] アクセス権 ファイル名` の順。`-R` [`--recursive`] でディレクトリ内すべて。

| 操作対象 | | 操作内容 | | 許可の種別 | |
|---|---|---|---|---|---|
| `u` | 所有者 (user) | `+` | 権限を追加 | `r` | 読み取り (4) |
| `g` | グループ (group) | `-` | 権限を削除 | `w` | 書き込み (2) |
| `o` | その他 (other) | `=` | 権限を指定 | `x` | 実行 (1) |
| `a` | 全て (all) | | | `s` | SUID / SGID |
| | | | | `t` | スティッキービット |

```
$ chmod go+w sample.txt   （＝ $ chmod 776 sample.txt ）
```

### umask

**デフォルトアクセス権 ＝ ファイル「666」／ディレクトリ「777」 − umask値**

| umask値 | ファイル | ディレクトリ |
|---|---|---|
| `0002`（一般的） | **664** | **775** |
| `0027` | **640** | **750** |

- `umask` だけで現在値の表示、`umask 027` で変更。下3桁が引かれる。

## SUID・SGID・スティッキービット（超頻出）

| 特殊permission | 数字 | 表示位置 | 意味 |
|---|---|---|---|
| **SUID** (Set User ID) | **4000** | 所有者の `x` が **`s`**（例 `-rwsr-xr-x`） | 実行権を持つユーザーが実行すると、**ファイルの所有者の権限**で実行される |
| **SGID** (Set Group ID) | **2000** | グループの `x` が **`s`**（例 `-rwxr-sr-x`） | 実行時に**所有グループの権限**で実行される。**SUIDと違いディレクトリにも設定できる** |
| **スティッキービット** | **1000** | その他の `x` が **`t`**（例 `drwxrwxrwt`） | 書き込み権があっても**他ユーザーが作成したファイルの削除・名前変更ができない**（rootは例外的に可） |

```
$ chmod u+s sample.txt   （= chmod 4755）  SUID
$ chmod g+s sample.txt   （= chmod 2755）  SGID
$ chmod o+t sampledir    （= chmod 1777）  スティッキービット
```

**代表例**

- `/usr/bin/passwd` は `-rwsr-xr-x`（SUID）。`/etc/shadow` はアクセス権が全く無い（`----------`）が、passwdコマンドがroot権限で実行されるため書き換えられる
- `/tmp` は `drwxrwxrwt`（スティッキービット）

## ファイル操作コマンド

### ls

| オプション | 意味 |
|---|---|
| `-a` [`--all`] | 「.」から始まるファイル（隠しファイル）も表示 |
| `-A` [`--almost-all`] | カレントディレクトリ（`.`）と親ディレクトリ（`..`）以外を表示 |
| `-i` [`--inode`] | **iノード番号**を表示 |
| `-l` [`--format=long`] | 詳細な情報を表示 |
| `-R` [`--recursive`] | サブディレクトリ内も含めて一括表示 |
| `-d` | 指定されたディレクトリ**だけ**の情報を表示 |

### その他

| コマンド | 内容 | 主なオプション |
|---|---|---|
| `file ファイル名` | **ファイルの種別**を表示（例：`ASCII text`） | |
| `cp コピー元 コピー先` | コピー | `-f`上書き／`-i`確認／`-p`属性を保持／**`-R`(`-r`) ディレクトリ全体**／`-d` リンク自体をコピー |
| `mv 移動元 移動先` | 移動（同一ディレクトリ内なら**名前変更**） | `-f`上書き／`-i`確認 |
| `mkdir ディレクトリ名` | 作成 | `-m`アクセス権指定／**`-p` 親ディレクトリも同時に作成** |
| `rm` | 削除 | `-f`確認なし／`-i`確認／**`-r`(`-R`) ディレクトリ内も全て** |
| `rmdir` | **空の**ディレクトリを削除（中身が残っていると削除不可） | `-p` 複数階層の空ディレクトリ |
| `touch ファイル名` | タイムスタンプ変更。**ファイルが無ければ空ファイルを作成** | `-t [CCYY]MMddhhmm[.SS]`／`-a`アクセス時刻のみ／`-m`修正時刻のみ |

- `cp -d` を指定しないと、リンクではなく**リンク元のファイル**がコピーされる
- `touch` で `-t` 省略時は**現在時刻**

## 圧縮・解凍

| コマンド | 圧縮ファイル末尾 | 解凍コマンド |
|---|---|---|
| `gzip` | `.gz` | `gunzip` |
| `bzip2` | `.bz2` | `bunzip2` |
| `xz` | `.xz` | `unxz` |

**圧縮率（と処理時間）：`xz` ＞ `bzip2` ＞ `gzip`**

`gzip` の主なオプション（bzip2も使い方は同じ）

| オプション | 意味 |
|---|---|
| `-c` [`--stdout`] | 標準出力へ出力する |
| `-d` [`--decompress`] | 圧縮ファイルを展開する |
| `-k` [`--keep`] | 圧縮・解凍後に元ファイルを削除しない |
| `-r` [`--recursive`] | ディレクトリ内のファイルをすべて圧縮（※ディレクトリ自体は圧縮しない） |

- **`-k` か `-c` を付けないと、元のファイルは削除される**
- `-d` オプションでも解凍できる

## tar（アーカイブ）

| オプション | 覚え方 | 内容 |
|---|---|---|
| `-c` | **c**reate | アーカイブを作成する |
| `-f` | **f**ile | アーカイブファイル名を指定する |
| `-r` | append | アーカイブにファイルを追加する |
| `-t` | lis**t** | アーカイブの中身を確認する |
| `-x` | e**x**tract | アーカイブからファイルを取り出す（展開） |
| `-z` | g**z**ip | gzipによる圧縮・展開をする |

- オプションのハイフンは省略できる（`tar cf` でも可）

```
$ tar -cf test.tar test1.txt test2.txt       作成
$ tar -tf test.tar                           中身確認
$ tar -xf test.tar                            展開
$ tar -czf test.tar.gz test.txt test_dir      作成＋gzip圧縮
```

## iノードとリンク

**iノード (index node)** … すべてのディレクトリ/ファイルを管理するデータ。属性情報（所有者・更新日時・サイズ）と、**ディスク上のどこに書き込まれているか**が記録されている。

確認：`ls -i`（iノード番号のみ）／`ls -li`（詳細＋iノード番号）

| | ハードリンク | シンボリックリンク |
|---|---|---|
| 実体との関係 | どれも同じ**ファイルの実体**に直接つながる | 実体にはつながらず、**実体につながっているファイル**につながる |
| iノード番号 | **すべて共通** | **それぞれ異なる** |
| 制限 | **異なるファイルシステムには作成できない**／**ディレクトリには作成できない** | 制限なし |
| 作成 | `ln リンク元 リンク名` | **`ln -s`** [`--symbolic`] |

- オプションを付けなければ**ハードリンク**が作られる

## ファイルの検索

**シェルのメタキャラクタ**

| 記号 | 意味 | 例 |
|---|---|---|
| `*` | **0文字以上**の文字・文字列 | `a*` → a, ab, abc, aaa |
| `?` | **任意の1文字** | `a?` → aa, ab, a1 |
| `[ ]` | 内にあるどれか1文字 | `a[bcd]` → ab, ac, ad／`[a-z]` `[0-9]` |
| `{ }` | 「,」で区切られた文字列 | `abc{123,789}` → abc123, abc789 |

| コマンド | 内容 |
|---|---|
| `find 検索対象ディレクトリ 検索式` | 指定ディレクトリ以下から条件に合うものを検索。**指定なしならカレントディレクトリ**。**アクセス権のある範囲しか検索できない** |
| `locate 検索パターン` | あらかじめ作成された**ファイル名データベース**から検索。**DB最終更新後に作成されたファイルは見つからない** |
| `updatedb` | ファイル名データベースを作成・更新（`-e パス` で除外パス指定） |
| `which コマンド名` | コマンドの**絶対パス**を表示 |
| `whereis コマンド名` | `-b`バイナリのみ／`-m`マニュアルのみ／`-s`ソースのみ を検索 |
| `type コマンド名` | 実行ファイルか**シェルの組み込みコマンド**かエイリアスかを表示 |

**find の検索式**

| 検索式 | 内容 |
|---|---|
| `-name ファイル名` | ファイル名で検索 |
| `-type ファイル種類` | **f**：ファイル、**l**：シンボリックリンク、**d**：ディレクトリ |
| `-user ユーザー名` | 所有者で検索 |
| `-exec コマンド {} \;` | 検索したファイルに対してコマンドを実行 |

## ファイルの配置（FHS）

Linuxのディレクトリ構造は **FHS（Filesystem Hierarchy Standard／ファイルシステム階層標準）** として標準化されている。

| ディレクトリ | 内容 |
|---|---|
| `/` | ルートディレクトリ。全てのディレクトリを格納 |
| `/bin` | **一般ユーザーでも実行可能**な基本コマンド。例：cat, chmod, cp, date, dd, kill, ls, mkdir, mv, ps, rm, su（RHEL7/CentOS7以降は `/usr/bin` へのシンボリックリンク） |
| `/sbin` | **rootユーザーのみ実行可能**なシステムに欠かせないコマンド。例：shutdown, fdisk, fsck, init, mkfs, mkswap, reboot（RHEL7以降は `/usr/sbin` へのシンボリックリンク） |
| `/etc` | 設定情報やスクリプトファイル |
| `/dev` | **デバイスファイル**。**udev** により自動作成される |
| `/lib` | 共通で使用する**ライブラリファイル** |
| `/media` | 外付けHDDやUSBメモリなどのマウント先 |
| `/proc` | システムの状態などの**仮想的なファイル**。HDD/SSD上ではなく**メモリ**の中に作られる |
| `/home` | ユーザーごとのホームディレクトリ（例：`/home/test`） |
| `/root` | **rootユーザーのホームディレクトリ** |
| `/boot` | Linuxカーネルなど**起動に必要なファイル** |
| `/tmp` | 一時的なファイルの置き場。**全ユーザーが読み書き可能** |
| `/var` | ログファイルなど**頻繁に更新されるファイル** |
| `/usr` | コマンドやライブラリなど |

`/usr` のサブディレクトリ

| ディレクトリ | 内容 |
|---|---|
| `/usr/bin` | システム起動に必須ではない、一般的に使うコマンド |
| `/usr/sbin` | システム起動に必須ではない、システム管理用コマンド |
| `/usr/lib` | 上記コマンドが共有するライブラリファイル |
| `/usr/local` | 設定ファイルなど（`/usr/local/bin`＝その他プログラム、`/usr/local/etc`＝その設定ファイル） |
| `/usr/share/man` | マニュアル |

---

# 主題1.03 GNUとUnixのコマンド

## プロセス

| 用語 | 意味 |
|---|---|
| プロセス | コンピュータによって処理されるプログラム。「プロセスの生成」＝「プログラムの実行」 |
| マルチタスク | 複数のプロセスを同時に稼働できるシステム |
| **デーモン (daemon)** | 一定の条件に従い**バックグラウンドで実行される常駐プロセス**。基本的に**語尾に `d`** が付く |

### ps

| オプション | 意味 |
|---|---|
| `a` | ユーザーが実行したすべてのプロセスを表示 |
| `f` | プロセスの**親子関係をツリー状**で表示 |
| `u` | プロセスの実行ユーザー名やその他の情報も表示 |
| `x` | ユーザーが制御していないサービス（**デーモン**）などのプロセスを表示 |
| `-e` | すべてのプロセスを表示 |
| `-f` | 起動時間なども含めた詳細情報（完全なフォーマット） |
| `-l` | 詳細情報を表示 |
| `-p` | **プロセスID**を指定して表示 |

- **ハイフンを付けないのがBSD形式、付けるのがUnix形式**
- `ps ax` ≒ `ps -ef`

表示項目：**PID**（プロセスID）／**TTY**（端末名）／**STAT**（プロセスの状態）／**TIME**（実行された総時間）／**COMMAND**（実行名）

### プロセスを確認するその他のコマンド

| コマンド | 内容 |
|---|---|
| `top` | 稼働中のプロセスを**リアルタイムで継続的に監視**。デフォルトは**CPU利用時間が長い順**。終了は **`Q`**。システム稼働状況だけなら **`uptime`** |
| `pgrep` | 特定のPIDを調べる。`-l` PIDに加えプロセス名も表示／`-U ユーザー名` 指定ユーザーのプロセス |
| `pstree` | プロセスの**親子関係を階層構造**で表示 |

### プロセスの終了

| シグナル | ID | 意味 |
|---|---|---|
| `HUP` | **1** | 端末切断による終了・再起動 |
| `INT` | **2** | キーボードによる割り込みで終了 |
| `KILL` | **9** | **強制終了** |
| `TERM` | **15** | **通常の終了（デフォルト）** |
| `CONT` | 18 | 一時停止中のプロセスを再開 |
| `STOP` | 19 | 一時停止 |
| `TSTP` | 20 | キーボードによる割り込みで一時停止 |

- `kill -l` でシグナル一覧を表示
- `kill 569` ＝ `kill -TERM 569` ＝ `kill -15 569`

| コマンド | 内容 |
|---|---|
| `kill PID` | **PID**を指定して終了 |
| `killall プロセス名` | 入力された名前と**一致する**プロセスを終了 |
| `pkill プロセス名` | その言葉が**含まれる**全てのプロセスを終了。`-U`ユーザー指定／`-G`グループ指定 |

## ジョブ

**ジョブ** … **シェル単位**で管理されているコマンドなどの処理のまとまり。

| | フォアグラウンドジョブ | バックグラウンドジョブ |
|---|---|---|
| 意味 | fore「前方の」 | back「後方の」 |
| 動作 | 終了するまで他の作業ができない | 裏で実行され、終了を待たずに次の入力ができる |

- 新規にバックグラウンドで実行：コマンドラインの最後に **`&`** （例：`sleep 60 &`）
- 実行中のジョブをバックグラウンドへ回す手順：
  1. **Ctrl + Z** で一時停止（サスペンド）
  2. **`jobs`** でジョブ番号を確認（`[1] + Stopped  sleep 60`）
  3. **`bg %1`**（フォアグラウンドに戻すなら **`fg %1`**）

## シェルの基本

| シェル | 説明 |
|---|---|
| `sh` (Bourneシェル) | **UNIXの標準シェル** |
| `bash` (Bourne Again シェル) | shを改良。**多くのディストリビューションの標準シェル** |
| `csh` (Cシェル) | C言語に似たスクリプトが使える |
| `tcsh` (TCシェル) | cshを拡張 |
| `ksh` (Kornシェル) | shを拡張 |
| `zsh` (Zシェル) | kshにbashやtcshの機能を追加した高機能シェル |

**シェルの基本操作**

| キー | 動作 |
|---|---|
| **Tab** | コマンドなどの残りを**補完**（候補が多ければ候補を挙げる） |
| Ctrl + **A** | 行の左端にカーソル移動 |
| Ctrl + **E** | 行の右端にカーソル移動 |
| Ctrl + **D** | 1文字削除、あるいは**ログアウト**（シェル終了） |
| Ctrl + **H** | カーソルの左を1文字削除（＝Backspace） |
| Ctrl + **L** | 画面をクリア |
| Ctrl + **C** | 処理を**強制終了** |
| Ctrl + **S** | 画面の更新を**停止** |
| Ctrl + **Q** | 画面の更新を**再開** |
| Ctrl + **Z** | 処理を**一時停止**（サスペンド） |

**ディレクトリを表すメタキャラクタ**

| 記号 | 意味 |
|---|---|
| `~` （チルダ） | 自分の**ホームディレクトリ** |
| `.` （ドット） | **カレントディレクトリ** |
| `..` （ドット×2） | 一つ上（**親**）のディレクトリ |

### set（シェルの設定）

| オプション | 意味 |
|---|---|
| `-o シェルオプション` | 指定したシェルオプションを**有効**にする |
| `+o シェルオプション` | 指定したシェルオプションを**無効**にする |
| `-o`（引数なし） | シェルオプションの有効・無効状態を一覧表示 |

- オプション無しの `set` のみ → **シェル変数やシェル関数を一覧表示**

**bashの主なシェルオプション**

| 名前 | 内容 |
|---|---|
| `allexport` | 作成・変更した変数を自動的に**エクスポート**する |
| `emacs` / `vi` | キーバインドをEmacs風／vi風にする |
| `history` | コマンド履歴を有効にする |
| `ignoreeof` | **Ctrl + D でログアウトできない**ようにする |
| `noclobber` | リダイレクトでファイルを**上書きしない** |
| `noglob` | メタキャラクタを使用したファイル名の表記をできないようにする |

## 変数

```
$ 変数名=値        定義（スペースを入れない！）
$ echo $変数名     参照（$ を付ける）
$ unset 変数名     削除（$ を付けない）
$ export 変数名    別のシェルでも使えるようにする（$ を付けない、同時に定義も可）
```

- 変数名の**最初に数字は使えない**。**大文字と小文字は区別される**
- `変数名` `=` `値` の間に**スペースを入れない**
- 値にスペースが入る場合は `'` か `"` で囲む
- `set -o allexport` で新規作成される変数が自動でエクスポートされる

| | シェル変数 | 環境変数 |
|---|---|---|
| 有効範囲 | 定義したシェルでのみ有効。別のシェルでは使えない | **複数のシェルやプロセスで共有される** |
| 終了時 | シェルを終了すると消える | シェルを終了しても値は保存される |

**代表的な環境変数**

| 環境変数 | 内容 |
|---|---|
| `EDITOR` | デフォルトで使用するテキストエディタのパス |
| `HISTFILE` | コマンド履歴が格納されているファイルのパス |
| `HISTFILESIZE` | **`.bash_history` に記録する**履歴の数（一般的に1000） |
| `HISTSIZE` | 使用中のシェルの履歴を**メモリ**に保存する履歴数 |
| `HOME` | カレントユーザーのホームディレクトリのパス |
| `LANG` | 使用する言語や文字コード（**ロケール**） |
| `PATH` | コマンドを実行する際に**検索するディレクトリのリスト** |
| `TERM` | 端末の種類 |
| `USER` | 現在のユーザーが誰なのか |

**パス**：`/`（ルート）からの道筋が**絶対パス**、カレントディレクトリなどからの道筋が**相対パス**

**内部コマンド**＝シェル自体に組み込まれているコマンド／**外部コマンド**＝独立したファイルとして存在するコマンド

**PATH** … 複数のパスが **`:`** で区切られ、シェルは**書かれている順に**コマンドを検索する。

```
$ PATH=$PATH:/opt/bin     ← 「:」を忘れると追加ではなく上書きになる
```

## コマンドの実行

| 書き方 | 意味 |
|---|---|
| `コマンドa ; コマンドb` | aが正常終了しようがしまいが、続いてbを実行 |
| `コマンドa && コマンドb` | aが**正常終了したときだけ**bを実行 |
| `コマンドa \|\| コマンドb` | aが**失敗したときだけ**bを実行 |
| `(コマンドa ; コマンドb)` | ひとまとまりのコマンドグループとして実行 |
| `{コマンドa ; コマンドb}` | **現在のシェル内で**実行 |

**引用符**

| 記号 | 名前 | 意味 |
|---|---|---|
| `'` | シングルクォーテーション | 囲まれているものは**全て文字列**として認識される |
| `"` | ダブルクォーテーション | 基本的に文字列だが、**`$変数名` は展開される** |
| `` ` `` | バッククォーテーション | 囲んだ**コマンドの実行結果**が展開される（`$(コマンド)` でも同じ） |

- `$` をそのまま使いたいときは **`\`（エスケープ文字）**

**コマンド履歴**

| 操作 | 意味 |
|---|---|
| `↑` / Ctrl + P | 新しいものから順にさかのぼって履歴を表示 |
| `↓` / Ctrl + N | 古いものから順に履歴を表示 |
| `!文字列` | その文字列**から始まる**コマンドを実行 |
| `!?文字列` | その文字列を**含む**コマンドを実行 |
| `!!` | **直前**のコマンドを再実行 |
| `!数字` | `history` の**履歴番号**のコマンドを実行 |
| `history` | コマンド履歴を表示 |

## マニュアルの参照（man）

**オンラインマニュアル**…「オンライン」だがインターネット上ではなく**コンピュータ上**にある。

| オプション | 意味 | 同等のコマンド |
|---|---|---|
| `-a` | 全て(all)のセクションのマニュアルを表示 | |
| `-f` | キーワードと**完全に一致**するドキュメントを表示 | **`whatis`** |
| `-k` | キーワードを**部分的に含む**ドキュメントを表示 | **`apropos`** |
| `-w` | マニュアルが格納されているディレクトリを表示 | |

- セクション番号を指定できる（例：`man 5 passwd`）

**manのセクション（章）番号**

| 番号 | 内容 |
|---|---|
| **1** | 一般ユーザーコマンド |
| **2** | システムコール |
| **3** | ライブラリ |
| **4** | デバイスファイル |
| **5** | 設定ファイル |
| **6** | ゲーム |
| **7** | その他 |
| **8** | システム管理コマンド |
| **9** | Linux独自のカーネル用ドキュメント |

**manページの見出し**：NAME（名前）／SYNOPSIS（書式）／DESCRIPTION（説明）／OPTIONS／FILES（関係するファイル）／ENVIRONMENT（環境変数）／NOTES（注意）／BUGS（既知の不具合）／SEE ALSO（関連項目）／AUTHOR（著者）

**manページ内での操作**

| キー | 動作 |
|---|---|
| `↑` / `k` | 上方向に1行スクロール |
| `↓` / `j` / `Enter` | 下方向に1行スクロール |
| `b` | 上方向に一画面分スクロール |
| **スペース** / `f` | 下方向に一画面分スクロール |
| `q` | マニュアルを終了 |
| `?文字列` | **上**方向に検索 |
| `/文字列` | **下**方向に検索 |
| `h` | ヘルプを表示 |
| `v` | エディタを起動 |

## テキスト処理（テキストフィルタ）

**フィルタ**…ファイルやテキストデータを読み込み、処理して出力するプログラム。

| コマンド | 内容 | 主なオプション |
|---|---|---|
| `cat` | ファイルの内容を標準出力に出力。**複数ファイルを1つにまとめられる** | `-n` [`--number`] 行番号を付ける |
| `nl` | 指定形式で行番号を付ける | `-b`本文／`-h`ヘッダ／`-f`フッタ。形式：`a`全ての行／`t`空白以外の行／`n`表示形式 |
| `od` | ファイルを特定の形式で出力。**バイナリをテキスト形式で見る**とき。**デフォルトは8進数** | `-t c`ASCII文字／`o`8進数／`x`16進数 |
| `head` | 先頭部分を表示。**デフォルト10行** | `-行数` |
| `tail` | 末尾部分を表示。**デフォルト10行** | `-行数`／**`-f` [`--follow`] リアルタイム表示**（終了はCtrl+C） |
| `cut` | 指定した位置を取り出す | `-c`文字位置／**`-d` 区切り文字（デリミタ）**／**`-f` フィールド番号** |
| `join` | 2ファイルを読み込み、共通フィールドがある行を**水平に連結** | `-j 番号` |
| `expand` | **タブ → スペース（8文字分）** に変換 | `-i` 行の最初のタブだけ／`-t` タブ幅 |
| `unexpand` | **行頭の空白 → タブ** に変換 | `-a` 行の最初以外の空白も／`-t` タブ幅 |
| `fmt` | 決まった文字数で改行（単語は途中で改行しない） | `-w 文字数` |
| `pr` | ファイルをページごとの形式に整える（印刷用） | `+開始:終了`／`-l 行数` |
| `tr` | 標準入力から読み込んだ文字列を変換・削除 | `-d` 削除／`-s` [`--squeeze-repeats`] 連続を1つにする |
| `sort` | 並び替え。**デフォルトは昇順**、基本的に1文字目でソート | `-b`行頭の空白無視／`-f`大小無視／**`-r`降順**／**`-n`数値として** |
| `split` | 指定行数でファイルを分割。**デフォルト1000行**。末尾に`aa`,`ab`,`ac`…が付く | `-行数` |
| `uniq` | **重複行を1つ**にする。**事前にソートされている必要がある** | `-d` 重複している行だけ／`-u` 重複していない行だけ |
| `wc` | 行数・単語数・文字数を表示 | `-c`バイト数／**`-l`行数**／`-w`単語数 |

**tr のクラス**：`[:alpha:]`英字／`[:lower:]`小文字／`[:upper:]`大文字／`[:digit:]`数字／`[:alnum:]`英数字／`[:space:]`スペース

## パイプとリダイレクト

| 番号 | 名前 | 意味 |
|---|---|---|
| **0** | 標準入力 | 何も指定されていない場合の**データの入力元**（キーボード） |
| **1** | 標準出力 | 何も指定されていない場合の**データの出力先**（モニター） |
| **2** | 標準エラー出力 | 何も指定されていない場合の**エラーの出力先** |

**リダイレクト一覧（超頻出）**

| 書き方 | 意味 |
|---|---|
| `コマンド > ファイル` | 実行結果をファイルに**上書き**保存 |
| `コマンド >> ファイル` | 実行結果をファイルに**追加**保存 |
| `コマンド < ファイル` | ファイルの内容をコマンドの**標準入力**に送る |
| `コマンド > ファイル 2>&1` | 標準出力と標準エラー出力をファイルに上書き保存 |
| `コマンド >> ファイル 2>&1` | 標準出力と標準エラー出力をファイルに追加保存 |
| `コマンド << 終了文字` | 終了文字が現れるまで標準入力へ送る（ヒアドキュメント） |
| `コマンド 2> ファイル` | **標準エラー出力**を上書き保存 |
| `コマンド 2>> ファイル` | **標準エラー出力**を追加保存 |
| `コマンド &> ファイル` | 同じファイルに標準出力と標準エラー出力を上書き保存 |

**パイプ `|`** … コマンドの実行結果を、他のコマンドの**入力として利用**する（例：`ls | wc`）

| コマンド | 内容 | オプション |
|---|---|---|
| `tee` | 実行結果を**出力するとともにファイルに書き込む** | `-a` 追加保存 |
| `xargs` | 受け取った文字列を**指定したコマンドの引数**にして実行 | |

```
$ ls | tee ls.txt | cat
$ ls -a | xargs cat
```

## テキスト検索（正規表現・grep・sed）

**正規表現のメタキャラクタ**（※シェルのメタキャラクタと使い方が違うので注意）

| 記号 | 意味 | 例 |
|---|---|---|
| `.` | 任意の**1文字** | `a.` → aa, ab, ac |
| `*` | **左側の文字を0回以上**繰り返し | `ab*c` → ac, abc, abbc |
| `[ ]` | 内のどれか1文字 | `a[abc]c` → aac, abc, acc |
| `[ - ]` | 使用する範囲を指定 | `a[a-c]c` |
| `[^ ]` | 指定した文字**以外** | `a[^a]c` → abc, acc |
| `^` | **行の最初** | `^a` |
| `$` | **行の末尾** | `a$` |
| `\` | 次の文字を普通の文字として処理（エスケープ） | `ab\*c` → ab*c |

- 正規表現として使いたいときは文字列を **`'`（シングルクォート）で囲む**

**拡張正規表現のメタキャラクタ**

| 記号 | 意味 | 例 |
|---|---|---|
| `+` | 左の文字を**1回以上**繰り返し | `ab+` → ab, abb, abbb |
| `?` | 左の文字を**0回または1回** | `ab?` → a, ab |
| `\|` | 左右どちらかにあうもの | `ab(cd\|dc)` → abcd, abdc |
| `{n}` | 左の文字を**n回**繰り返し | `a{5}` → aaaaa |
| `{n,m}` | 左の文字を**n〜m回**繰り返し | `a{4,5}` → aaaa, aaaaa |

### grep

| オプション | 意味 |
|---|---|
| `-c` [`--count`] | パターンがあった**行数**を表示 |
| `-f` [`--file`] ファイル名 | パターンをファイルから読み込む |
| `-i` [`--ignore-case`] | 大文字と小文字を区別しない |
| `-n` [`--line-number`] | **行番号**も表示 |
| `-v` [`--invert-match`] | 検索内容が**なかった行**を表示 |
| `-E` [`--extended-regexp`] | **拡張正規表現**を使用 |

- 拡張正規表現には **`egrep`**、正規表現を使わないなら **`fgrep`** も使える

### sed

| オプション | 意味 |
|---|---|
| `-f` | コマンドが書かれたスクリプトファイルを用いる |
| `-i` | 編集内容を**ファイルに上書きする** |

- **`-i` を指定しない限りファイルは上書きされない**

| sedのコマンド | 意味 |
|---|---|
| `d` | 行を**削除**する |
| `s` | ある文字列を別の文字列に**置換**する |
| `y` | 文字を**変換**する |

```
$ sed s/abc/ABC/ test1.txt      各行で最初に出てきた abc を ABC に置換
$ sed s/abc/ABC/g test1.txt     出てきた全ての abc を置換（g = global）
$ sed '1,8d' test1.txt > test2.txt   1〜8行目を削除
$ sed y/abc/123/ test1.txt      a→1, b→2, c→3 に変換
```

## viエディタ

- **UNIXで標準的に使われている**テキストエディタ。拡張したものが **Vim**、他に **Emacs**
- **入力モード**と**コマンドモード**を切り替えながら使用する（**Esc** でコマンドモードへ）
- `vi ファイル名`。ファイル名を指定しなければ新しいファイルが開かれる
- エディタの切り替え：`export EDITOR=/usr/bin/vim`

**設定**

| コマンド | 意味 |
|---|---|
| `:set nu` | 行番号を表示 |
| `:set nonu` | 行番号を表示しない |
| `:set ts=数字` | Tabキーで挿入される空白の数を指定 |

**カーソル移動**

| キー | 移動先 |
|---|---|
| `h` / `j` / `k` / `l` | 左 / 下 / 上 / 右（←↓↑→でも可） |
| `0`（ゼロ） | 行の**最初** |
| `$` | 行の**最後** |
| `gg` | ファイルの**先頭** |
| `G` | ファイルの**末尾** |
| `数字G` または `:数字` | 指定した行番号 |

**入力**

| キー | 動作 |
|---|---|
| `i` | カーソルのある場所（**左側**）から入力 |
| `a` | カーソルの**右側**から入力 |
| `I`（アイ） | **行の最初**に移動して入力 |
| `A` | **行の末尾**に移動して入力 |
| `o` | カーソル行の**すぐ下**に空行を挿入して入力 |
| `O` | カーソル行の**すぐ上**に空行を挿入して入力 |

**編集**

| キー | 動作 |
|---|---|
| `x` | カーソル位置の文字を削除 |
| `X` | カーソルの**左側1文字**を削除 |
| `dd` | 現在の行をまるごと削除 |
| `dw` | カーソル位置から次の単語まで削除 |
| `yy` | 現在の行をコピー（バッファへ） |
| `p` | 現在の行の**下**に貼り付け |
| `P` | 現在の行の**上**に貼り付け |

**保存・終了**

| コマンド | 動作 |
|---|---|
| `:w` | 上書き保存 |
| `:wq` | 上書き保存して終了 |
| `ZZ` | `:wq` と同じ |
| `:q!` | **保存せずに終了** |
| `:q` | 終了（変更内容がある場合は確認される） |
| `:!コマンド名` | viを終了せずコマンドを実行 |

**検索・置換**

| コマンド | 動作 |
|---|---|
| `/文字` | **下**へ検索 |
| `?文字` | **上**へ検索 |
| `n` / `N` | 次（下） / 前（上）の候補 |
| `:noh` | ハイライトを解除 |
| `:%s/A/B/` | 各行で**最初に見つかった** A を B に置換 |
| `:%s/A/B/g` | ファイル内の**すべての** A を B に置換 |

---

# 主題1.04 リポジトリとパッケージ管理

## パッケージの基本

**パッケージ**＝ソフトウェアに必要なもの（実行ファイル・ライブラリ・設定ファイル・マニュアルなど）を1つにまとめたファイル

| 問題 | 内容 |
|---|---|
| **依存関係** | パッケージAのファイルをパッケージBが利用している |
| **競合関係** | パッケージAとBのファイルが共存できない（バージョンの違いなど） |

**リポジトリ**＝様々なファイルやデータが保管されているデータベース

| | Debian形式 | RPM形式 |
|---|---|---|
| ディストリビューション | **Debian GNU/Linux, Ubuntu** など | **RHEL, CentOS, Fedora, openSUSE** など |
| 高レベルコマンド | **`apt`** / `apt-get` / `apt-cache` | **`yum`**（最近は `dnf`） |
| 低レベルコマンド | **`dpkg`** | **`rpm`** |

## APT（Debian系）

**APT (Advanced Packaging Tool)** … 多機能だが操作が煩雑な dpkg に代わり操作性を向上させたパッケージ管理システム。

| コマンド | 用途 |
|---|---|
| `apt` | インストールから検索まで幅広く。**`apt-get`/`apt-cache` より apt が推奨** |
| `apt-get` | インストール・アンインストール・アップグレード |
| `apt-cache` | パッケージの情報を調べる・検索する（**未インストールも検索可。root権限不要**） |
| `apt-file` | 指定したファイルを含むパッケージを検索 |

### apt-get / apt のオプション

| オプション | 意味 |
|---|---|
| `-s` [`--dry-run`] | システムは変更せず**動作をシミュレート** |
| `-y` [`--yes`] | 問い合わせに自動的に yes と答える |
| `-d` | ダウンロードのみ行う（サブコマンドは install） |
| `-c 設定ファイル` | 設定ファイルを指定 |
| `--reinstall` | 再インストールを許可 |

### apt-get の主なサブコマンド

| サブコマンド | 内容 |
|---|---|
| `install` | インストール または アップグレード |
| `remove` | アンインストール |
| `update` | リポジトリから**パッケージの最新情報を取得**（インストール/アップグレードの前に必要） |
| `upgrade` | 他のパッケージを**追加・削除しない**ものをアップグレード |
| `dist-upgrade` | システム（ディストリビューション）を最新にアップグレード |
| `autoclean` | ダウンロードしたパッケージのアーカイブを削除 |
| `autoremove` | 自動インストールされて**もう使われていない**パッケージを削除 |
| `download` | ダウンロードするだけでインストールしない |

### apt の主なサブコマンド（apt-getとの違いに注意）

| サブコマンド | 内容 |
|---|---|
| `install` / `remove` | インストール／アンインストール（**設定ファイルは削除しない**） |
| **`purge`** | アンインストールして**設定ファイルも削除**（完全削除） |
| `update` | パッケージ情報のリストを更新 |
| `upgrade` | パッケージ削除が伴わないものをアップグレード |
| `full-upgrade` | 全てのパッケージを完全にアップグレード |
| `autoremove` | 使われていないパッケージを削除 |
| `list` | パッケージのリストを表示（`--installed` でインストール済み） |
| `search` | キーワードで検索 |
| `depends` | **依存関係**を表示 |

### apt-cache の主なサブコマンド

| サブコマンド | 内容 |
|---|---|
| `search キーワード` | キーワードが含まれたパッケージを検索 |
| `show パッケージ名` | パッケージの情報を表示 |
| `showpkg パッケージ名` | 詳細な情報を表示 |
| `depends パッケージ名` | **依存関係**を表示 |

### APTのリポジトリ設定ファイル

- **`/etc/apt/sources.list`** ファイル
- **`/etc/apt/sources.list.d`** ディレクトリ内のファイル

```
deb http://archive.ubuntu.com/ubuntu focal multiverse
 ①            ②                        ③      ④
① deb（パッケージ取得）か deb-src（ソース取得）か
② 取得先の URI
③ バージョン名
④ main（公式サポート）/ universe（コミュニティ）/ multiverse（制限あり）
```

### apt-file

- `-F` [`--fixed-string`] 完全一致のみ
- 標準ではインストールされていないので `apt-file` パッケージの導入が必要
- 使用前に **`apt-file update`** でパッケージ情報を更新

## dpkg（Debian形式）

**パッケージファイル名の構成**

```
tree_1.7.0-5_amd64.deb
 ①    ②      ③    ④
① パッケージ名  ② バージョン情報（とディビジョン番号）
③ CPUのアーキテクチャ  ④ 拡張子（.deb = Debian系）
```

**dpkg のオプション**

| オプション | 意味 |
|---|---|
| `-E` | 同じバージョンがすでにインストール済みならインストールしない |
| `-G` | 新しいバージョンがすでにインストール済みならインストールしない |
| `-R` [`--recursive`] | ディレクトリ内を再帰的に処理 |

**dpkg の主なアクション**

| アクション | 内容 |
|---|---|
| `-i` [`--install`] | インストール |
| `-r` [`--remove`] | **設定ファイルは残して**削除 |
| `-P` [`--purge`] | **設定ファイルも含めて完全に**削除 |
| `-l` [`--list`] | インストール済みパッケージを（検索して）表示 |
| `-S` [`--search`] | 指定したファイルが**どのパッケージからインストールされたか**を表示 |
| `-L` [`--listfiles`] | 指定パッケージから**インストールされたファイルを一覧表示** |
| `-s` [`--status`] | 指定パッケージの情報を表示 |
| `--unpack` | パッケージを展開する（インストールはしない） |
| `--configure` | 展開されたパッケージを構成（設定）する |
| `--reconfigure` | インストール済みパッケージを再設定する |

## YUM（RedHat系）

**YUM (Yellow dog Updater, Modified)** … CentOSやFedoraで用いられる、**APTに相当する**管理ツール。

**設定ファイル**：**`/etc/yum.conf`** ファイル、**`/etc/yum.repos.d`** ディレクトリ

- 最近は `yum` に代わって **`dnf`** が広がってきている
- 複数パッケージをまとめた**グループ**単位でインストールできる

| サブコマンド | 内容 |
|---|---|
| `check-update` | アップデート待ちのパッケージリストを表示 |
| **`update`** | 指定したパッケージを**アップデート**する（※aptの`update`とは意味が違う！） |
| `install` / `remove` | インストール／アンインストール |
| `info` | パッケージの情報を表示 |
| `list` | リポジトリにある全パッケージの情報を一覧表示 |
| `repolist` | リポジトリ一覧を表示 |
| `search` | キーワードで検索 |
| `groups list` | パッケージグループを一覧表示 |
| `groups install` | 指定グループのパッケージをまとめてインストール |

## RPM

**RPM (Red Hat Package Manager)** … Red Hat社が開発したパッケージ管理システム。

```
bash-5.1.16-1.x86_64.rpm
 ①    ②    ③  ④     ⑤
① パッケージ名 ② バージョン情報 ③ リリース番号 ④ CPUアーキテクチャ ⑤ 拡張子
```

**インストール・アップグレード系**

| オプション | 意味 |
|---|---|
| `-i` [`--install`] | インストール |
| `-U` [`--upgrade`] | アップグレード（**未インストールならインストールする**） |
| `-F` [`--freshen`] | インストール済みならアップグレード（**なければ無視**） |

**併用オプション**

| オプション | 意味 |
|---|---|
| `-v` | 詳細な情報を表示 |
| `-h` [`--hash`] | 進捗状況を **`#`（メーター）** で表示 |
| `--nodeps` | **依存関係を無視**して処理 |
| `--force` | すでに存在するファイルを新しいものに置き換える |
| `--test` | 実際にはインストールせず仮テストを実施 |

`# rpm -ivh bash-5.1.16-1.x86_64.rpm` … **i**nstall + **v**erbose + **h**ash

**アンインストール**：`-e` [`--erase`]（`--nodeps` 併用可）

**検索・表示（`-q`）と併用オプション**

| オプション | 意味 |
|---|---|
| `-q` | 指定パッケージがインストールされているか調べる |
| `-a` [`--all`] | インストールされている**全ての**パッケージを表示 |
| `-f ファイル名` | 指定したファイルを含む**パッケージ名**を表示 |
| `-c` [`--configfiles`] | **設定ファイルだけ**を表示 |
| `-d` [`--docfiles`] | **ドキュメントのみ**を表示 |
| `-i` [`--info`] | 指定パッケージの**情報**を表示 |
| `-l` [`--list`] | 指定パッケージに**含まれるファイル**を表示 |
| `-R` [`--requires`] | 指定パッケージが**依存しているファイル**を表示 |
| `--changelog` | 変更履歴を表示 |

※ `-i` は install（インストールモード）と info（`-q` 併用時）で意味が変わるので注意

---

# 主題1.05 ハードウェア、ディスク、パーティション、ファイルシステム

## ハードウェアの基本

| 部品 | ポイント |
|---|---|
| **CPU** | 演算処理を行う「コンピュータの頭脳」。1コアで一度に処理できるプロセスは本来1つ。**スレッド**で見かけ上複数処理可。性能指標は動作周波数 **GHz**。種別は**アーキテクチャ**（Intel：**x86 / x86-64 / IA-32**、他に **ARM**） |
| **マザーボード** | CPUソケットに対応するCPUしか搭載できない。**BIOSまたはUEFIが組み込まれている** |
| **メモリ** | データを**一時的**に記録。**電源が切れると内容が失われる**。Linuxの動作には1GB前後必要。一般に **RAM** と呼ばれる |
| **ストレージ** | データを**長期的**に保存。OSの起動ディスクにできる。**磁気ディスク（プラッタ）＝HDD**／**フラッシュメモリ＝SSD**。HDDは壊れやすいが安価、SSDは壊れにくいが高価 |
| **入力装置** | キーボードとマウス。入力が不要なら未接続でもよい |
| **拡張カード** | 拡張スロットに装着。ネットワークカード、RAIDカード、ビデオカード（グラフィックボード）。**取り付け・取り外しは電源を切ってから** |

**ストレージの接続規格**

| 規格 | 説明 |
|---|---|
| **SATA** (Serial ATA) | 現在広く使われている |
| **SAS** (Serial Attached SCSI) | SATAより高速・高信頼だが高価 |
| SCSI | 昔は周辺機器接続に使われた。現在はあまり使われない |
| **NVMe** | SSDで使われる規格。**SATAより高速** |
| USB | SCSIに代わり周辺機器接続に広く使われる |
| IDE | SATA普及前の旧式規格 |

## デバイスの管理

**デバイスファイル** … 全てのハードウェアは **`/dev`** ディレクトリ内にデバイスファイルとして管理される。**udev** により自動作成。

| ディレクトリ | 内容 |
|---|---|
| `/dev` | デバイスファイルが置かれる |
| `/proc` | 接続されたハードウェアの状態に関する（仮想的な）ファイル |
| `/sys` | テキストファイルとしてシステム情報にアクセスできる（仮想的な）ファイル |

| ファイル | 内容 |
|---|---|
| `/proc/cpuinfo` | CPUの情報 |
| `/proc/interrupts` | **IRQ (Interrupt Request)** ＝割り込み処理の情報 |
| `/proc/ioports` | CPUとその他デバイスが通信するためのアドレス情報 |
| `/proc/meminfo` | メモリの情報 |
| `/proc/swaps` | **スワップ領域**の情報 |
| `/proc/bus/usb/デバイス名` | USBデバイスの情報 |
| `/proc/bus/pci/デバイス名` | PCIデバイス（拡張カード）の情報 |

**確認コマンド**

| コマンド | 内容 |
|---|---|
| `cat /proc/cpuinfo` / `lscpu` | CPU情報 |
| `lspci -v` | PCIデバイスの情報（詳しく） |
| `lsusb -t` | USBデバイスの情報（ツリー状） |
| `lsmod` | 組み込まれている**カーネルモジュール**を確認 |
| `modprobe` | デバイスドライバを**手動でロード**（依存関係も解決） |
| `insmod` / `rmmod` | ロード／取り外し |

**USB**

- 最大 **127台** まで接続できる
- 複数デバイスを1つのコネクタで接続、電源を入れたまま着脱可、**プラグ＆プレイ**、ポートから電源供給可

| 規格 | 速度 |
|---|---|
| USB 1.0 / 1.1 | **12Mb/秒** |
| USB 2.0 | **480Mb/秒** |
| USB 3.0 | **5Gb/秒** |
| USB 3.1 | **10Gb/秒** |
| USB 3.2 | **20Gb/秒** |

**デバイスドライバ** … デバイスを利用するための制御プログラム。Linuxでは**カーネルの一部（カーネルモジュール）**として組み込まれる。組み込むことを「**ロードする**」という。

**udev の仕組み（順序）**

1. デバイスが接続される
2. **カーネル**が **`/sys`** にデバイス情報を作成する
3. **udevデーモン (udevd)** がその情報を使って **`/dev`** にデバイスファイルを作成する
4. デバイス情報が **D-Bus** によってアプリケーションに伝えられる

※ udev の設定ファイルは **`/etc/udev/rules.d`**。編集でデバイスファイル名を変更できる。

## パーティション

**パーティション** … HDD/SSD内に作成された**論理的な区分け**。パーティションごとに異なるファイルシステムを作成できる。

- メリット：不具合が他のパーティションに影響しにくい／大量データが他を圧迫しない

**BIOS（MBR）の場合**

| 種類 | ポイント |
|---|---|
| 基本パーティション | 1ディスクに**最大4つ**まで。デバイス名は **`/dev/sda1〜4`** |
| 拡張パーティション | 1ディスクに**1つだけ**。基本パーティションと**合わせて4つまで**。論理パーティションを格納するためのもの |
| 論理パーティション | 拡張パーティション内に複数作成可。デバイス名は **`/dev/sda5〜`** |

**UEFI（GPT）の場合**

| 種類 | ポイント |
|---|---|
| **EFIシステムパーティション (ESP)** | UEFIシステムで作成される。管理用ファイルが置かれる。**最小サイズ100MB** |
| 基本パーティション | `/dev/sda1〜4` |

※ **UEFIでは「拡張パーティション」「論理パーティション」は存在しない**

**パーティションテーブル**（記憶領域の先頭にあり、最初に読み込まれる）

| 方式 | システム | 扱える容量 | 作成できるパーティション数 |
|---|---|---|---|
| **MBR**（マスターブートレコード） | BIOS | **2TB** | 基本パーティション **4個** |
| **GPT**（GUIDパーティションテーブル） | UEFI | — | **128個** |

**LVM (Logical Volume Manager)**

- 従来より柔軟にディスクを管理できる
- 仮想的なパーティションを作成して**ボリューム**を管理
- 作成した**論理ボリューム**はサイズ変更や他ディスクへの移動ができる
- 容易にバックアップが作成できる（**スナップショット**）

**記憶装置のデバイスファイル**

| デバイスファイル | 内容 |
|---|---|
| `/dev/sda` 〜 `/dev/sdd` | 1〜4番目のHDD・SSD・フラッシュメモリ |
| **`/dev/sr0`** | 1番目の **CD/DVDドライブ** |
| **`/dev/st0`** | 1番目の**テープドライブ** |

## パーティションの操作コマンド

| コマンド | 特徴 |
|---|---|
| **`fdisk`** | `-l` でパーティションテーブルの状態を表示。**`-l` なしで実行するとコマンド形式の実行モード**に入る。**最後に `w` を実行しないと反映されない** |
| **`gdisk`** | **GPT** に対応。使い方は基本的に fdisk と同じ |
| **`parted`** | **MBR・GPT両方**を操作できる。**変更が即座に反映される**（編集状態が存在しない）。`-s`[`--script`] で実行モードに入らず直接サブコマンド指定 |

**fdisk の主なサブコマンド**

| サブコマンド | 内容 |
|---|---|
| `l`（エル） | パーティションタイプを一覧表示 |
| `n` | パーティションを**作成** |
| `d` | パーティションを**削除** |
| `p` | パーティションテーブルを**表示** |
| `t` | パーティション**タイプを変更** |
| `a` | **ブートフラグ**のオン/オフ切り替え |
| **`w`** | **変更を保存して終了** |
| `q` | 保存しないで終了 |
| `m` | ヘルプメニュー |

**parted の主なサブコマンド**

| サブコマンド | 内容 |
|---|---|
| `check パーティション番号` | ファイルシステムのチェック |
| `mklabel 種類` | 新しいパーティションテーブルを作成 |
| `mkpart 種類 開始 終了` | パーティションを作成 |
| `rm パーティション番号` | 削除 |
| `p` [`print`] | パーティションテーブルを表示 |
| `q` [`quit`] | 終了 |

## ファイルシステム

ディスクは「**セクタ**」という区画に分かれ、ファイルシステムは「**ブロック**」という単位でデータを管理する。

| 種類 | 特徴 |
|---|---|
| `ext1` | 1992年公開。Linuxに搭載された**最初の**ファイルシステム |
| `ext2` | ext1の翌年公開。広く普及 |
| `ext3` | ext2に**ジャーナル（ログ）機能**を加えたもの |
| `ext4` | ext3の機能を拡張した後継 |
| **`XFS`** | **RHEL/CentOSのデフォルト** |
| `Btrfs` | 自己修復機能など新機能が追加されている新しいFS |
| `VFAT` | **SDカードやUSBメモリ**などフラッシュメモリで使用 |
| `tmpfs` | **メモリ上**に作成できるFS。高速だが電源を切ると消える |

**Btrfs の特徴**

1. 仮想ボリュームを作成できる（**ストレージプール**）
2. 複数の物理ボリュームをまたがる1つのFSを作成できる（**マルチデバイスファイルシステム**）
3. ある時点の状態を記録してバックアップできる（**スナップショット**）
4. 1つのFSをさらに分割した**サブボリューム**を作成できる

### ファイルシステムの作成

| コマンド | 内容 | オプション |
|---|---|---|
| `mkfs` | パーティションにファイルシステムを作成。**rootユーザーのみ実行可** | `-t` 種類を指定／`-c` 不良ブロックを検査 |
| `mke2fs` | **ext2 / ext3 / ext4** を作成。**指定なしなら ext2** | `-t` 種類／**`-j` ext3を作成**／`-c` 不良ブロック検査 |
| `mkfs.btrfs` | Btrfsを作成（`mkfs -t Btrfs` で呼ばれるプログラム） | |
| `mkfs.xfs` | XFSを作成 | |

- `mkfs -t ext4` は内部で **`mkfs.ext4`** を呼び出している

**XFS関連コマンド**

| コマンド | 内容 |
|---|---|
| `xfs_info` | ファイルシステムの情報を表示 |
| `xfs_db` | デバッグを行う |
| `xfs_check` | チェックする |
| `xfs_admin` | パラメータを変更（ボリュームラベルなど） |
| `xfs_fsr` | **デフラグ**を行う |
| `xfs_repair` | 検査・修復する |

## スワップ領域

**スワップ領域** … HDD/SSDのパーティションを**メモリの代わり**として利用する領域。

- メモリ不足でシステム全体が止まるのを防げる
- 本物のメモリよりアクセス速度は劣る
- 基本的にLinuxインストール時に作成される。**最低1つは必要**
- 本物のメモリ8GB＋スワップ1GB → 利用されるメモリ9GB

| コマンド | 内容 |
|---|---|
| `mkswap デバイス名/ファイル名` | スワップ領域を**作成**（**ファイルもスワップ領域にできる**） |
| `swapon` | スワップ領域を**有効**にする。**`-s` で有効化されている領域を一覧確認** |
| `swapoff` | スワップ領域を**無効**にする |

- 有効なスワップ領域は **`cat /proc/swaps`** でも確認できる

## マウントとアンマウント

**マウント** … あるファイルシステムに別のファイルシステムを組み込むこと。**マウントしないとシステムはそのファイルシステムを利用できない**。接続するディレクトリを**マウントポイント**、取り外すことを**アンマウント**という。

### mount

| オプション | 意味 |
|---|---|
| `-a` | **`/etc/fstab`** にあるファイルシステムを**全てマウント** |
| `-t ファイルシステム名` | ファイルシステムの種類を指定 |
| `-o` | マウントオプションを指定 |

- オプション無しの `mount` 単体 → **現在のマウント状況を表示**
- デバイスかマウントポイントを指定しない場合は **`/etc/fstab`** が参照される
- **マウント・アンマウントは rootユーザーのみ**

```
# mount -t ext3 /dev/sdc1 /data
```

### /etc/fstab

ファイルシステムの情報が記述されており、マウント/アンマウント時に参照される。頻繁にマウントするFSを記載しておく。

```
/dev/sda2   /usr   ext4   defaults   1   2
   ①        ②      ③       ④       ⑤   ⑥
① デバイスファイル名 または ラベル名 または UUID
② マウントポイント
③ ファイルシステムの種類
④ マウントオプション
⑤ dumpフラグ（dumpコマンドのバックアップ対象か）1が対象 / 0が対象外
⑥ fsckのチェック順序（ルートファイルシステムは1、0はチェックしない）
```

**主なマウントオプション**

| オプション | 内容 |
|---|---|
| `async` | 非同期入出力を設定 |
| `auto` | **`mount -a`** でマウントする |
| `noauto` | `mount -a` でもマウントされない |
| `defaults` | デフォルトのオプションを設定 |
| `exec` / `noexec` | バイナリの実行を許可する／しない |
| `ro` / `rw` | 読み取り専用／読み書き許可 |
| `user` | 一般ユーザーがマウントできる |
| `users` | マウントしていないユーザーでもアンマウントできる |
| `nouser` | **rootユーザーのみ**マウントできる |

### umount

| オプション | 意味 |
|---|---|
| `-a` | `/etc/fstab` にあるファイルシステムを全てアンマウント |
| `-t ファイルシステム名` | その種類だけをアンマウント |

- **デバイス内のデータをユーザーが利用中の場合はアンマウントできない**

```
# umount /data
# umount -at btrfs
```

---

# 直前チェック：数字とパスだけ

## 数字

| 値 | 意味 |
|---|---|
| 4 / 2 / 1 | r（読む＝４む）／w（two）／x（実行するワン） |
| 666 / 777 | ファイル／ディレクトリのデフォルトアクセス権 |
| **4000 / 2000 / 1000** | SUID／SGID／スティッキービット |
| 0 / 1 / 2 | 標準入力／標準出力／標準エラー出力 |
| 1 / 2 / 9 / 15 | シグナル HUP／INT／KILL／TERM |
| 1〜9 | manのセクション（1一般コマンド、5設定ファイル、8システム管理コマンド） |
| 10行 | head / tail のデフォルト表示行数 |
| 1000行 | split のデフォルト分割行数 |
| 4個 / 1個 | MBRの基本パーティション最大数／拡張パーティション数 |
| 128個 | GPTで作成できるパーティション数 |
| 2TB | MBRが扱えるディスク容量 |
| 100MB | EFIシステムパーティション(ESP)の最小サイズ |
| 127台 | USBの最大接続台数 |
| 1991年 | Linux公開（リーナス・トーバルズ、ヘルシンキ大学） |

## パス・ファイル

| パス | 内容 |
|---|---|
| `/etc/ssh/sshd_config` | SSHサーバ側の設定（`X11Forwarding yes` もここ） |
| `~/.ssh/authorized_keys` | 登録された公開鍵のリスト |
| `~/.ssh/id_rsa` / `id_rsa.pub` | クライアントの秘密鍵／公開鍵 |
| `~/.ssh/known_hosts` | 接続先サーバの情報 |
| `~/.Xauthority` | X接続の資格情報 |
| `/etc/shadow` | パスワード情報（アクセス権なし。passwdのSUIDで書き換え） |
| `/etc/fstab` | ファイルシステムの情報（マウント時に参照） |
| `/etc/apt/sources.list` | APTのリポジトリ設定 |
| `/etc/yum.conf` / `/etc/yum.repos.d` | YUMの設定 |
| `/etc/udev/rules.d` | udevの設定 |
| `/proc/cpuinfo` / `/proc/meminfo` / `/proc/swaps` | CPU／メモリ／スワップの情報 |
| `/dev/sda1〜4` / `/dev/sda5〜` | 基本パーティション／論理パーティション |
| `/dev/sr0` / `/dev/st0` | CD/DVDドライブ／テープドライブ |

## 混同しやすいペア

| | |
|---|---|
| **Xサーバ**＝入出力装置側（手元） | **Xクライアント**＝アプリ側（リモート） |
| `apt update`＝**情報の更新** | `yum update`＝**パッケージのアップデート** |
| `apt remove`＝設定ファイルは残す | `apt purge` / `dpkg -P`＝設定ファイルも削除 |
| `rpm -i`＝インストール | `rpm -qi`＝情報表示 |
| ハードリンク＝iノード共通、FS跨ぎ不可、ディレクトリ不可 | シンボリックリンク＝iノード別 |
| `systemctl restart`＝設定を再読み込みしない | `systemctl reload`＝再読み込みする |
| `fdisk` / `gdisk`＝`w` で保存が必要 | `parted`＝即座に反映 |
| `find`＝実際に探す | `locate`＝DBから探す（`updatedb`が必要） |
| シェルの `*`＝0文字以上の文字列 | 正規表現の `*`＝左の文字を0回以上繰り返し |
| `expand`＝タブ→スペース | `unexpand`＝行頭のスペース→タブ |
| `ln`（オプション無し）＝ハードリンク | `ln -s`＝シンボリックリンク |
| `which`＝コマンドの絶対パス | `type`＝内部コマンド/外部/エイリアスの区別 |
